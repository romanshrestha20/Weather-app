<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather Forecast App</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
        integrity="sha512-dySvmf+wkq8gM65t1G9WuxwB5wRVsY2e5w2vYmKUdIcDWvWe0ltLJlG4kpMa6J7J2UuyCflIteY/jPjTf1W5lQ=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link rel="stylesheet" href="css/styles.css">
</head>

<body>
    <div class="container mx-auto p-4 dark:bg-slate-800">
        <h1 class="text-3xl font-semibold mb-4">Weather Forecast</h1>

        <div class="form mb-4">
            <form id="weatherForm" class="flex">
                <input type="text" id="cityInput" name="city" placeholder="Enter city name"
                    class="flex-1 px-4 py-2 rounded-lg shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-400"
                    autocomplete="off">
                <button type="submit"
                    class="ml-2 bg-blue-500 hover:bg-blue-600 text-white font-semibold py-2 px-6 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-400">
                    Get Weather
                </button>
            </form>
            <button id="getLocationBtn"
                class="mt-2 bg-gray-300 hover:bg-gray-400 text-gray-800 dark:text-slate-400 font-semibold py-2 px-6 rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-400">
                Use My Location
            </button>
        </div>

        <div id="loadingIndicator" class="text-gray-600">Loading...</div>

        <div class="weather-info mt-6  ">
            <!-- Weather data will be displayed here -->
            
        </div>

        <div class="day-container mt-6">
            <h2 class="text-xl font-semibold mb-2">5-Day Forecast</h2>
            <div id="forecastContainer" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
                <!-- Forecast items will be added dynamically here -->
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <script src="js/index.js"></script>
</body>

</html>
